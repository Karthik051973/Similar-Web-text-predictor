# Similar-Web-text-predictor
A Streamlit-based NLP app that crawls multiple URLs, extracts clean text, and analyzes content using TF-IDF and similarity techniques (cosine + Jaccard). It compares web pages, clusters similar content, and displays top keywords with importance scores.
